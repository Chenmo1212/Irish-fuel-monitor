location:
  latitude: 53.2753
  longitude: -6.203
  radius_km: 20

fuel:
  # petrol = E10 (cheaper, most common)
  # petrolplus = E5/98 (premium, higher octane)
  # diesel = B7
  # dieselplus = B7 Premium
  type: petrol

vehicle:
  typical_fill_litres: 40

prediction:
  horizons:
    - 1
    - 2
    - 3
    - 7

alert:
  minimum_score: 0
  cooldown_hours: 24
  significant_price_drop_cents: 1.0

notification:
  enabled: true
  # source options: "telegram" | "fuel-monitor" (custom HTTP API)
  source: telegram
  # test_mode: bypasses score threshold and cooldown — forces a notification send
  test_mode: true